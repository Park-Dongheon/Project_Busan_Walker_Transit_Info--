// src/domains/map/types/index.ts

/**
 * types/index.ts (Map Domain - 타입 배럴)
 *
 * map 도메인 타입 모듈의 단일 진입점.
 * public(외부 계약)과 viewModel(UI 가공 타입)을 한 경로로 통합하여 재노출
 */

export * from './public'
export * from './viewModel'